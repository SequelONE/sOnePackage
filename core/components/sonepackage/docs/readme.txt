sOnePackage contains common functionality that is shared between MODX packages developed by sequelone. Those other packages require sOnePackage to be installed, so it is automatically installed for you when installing such a package.

Packages that depend on sOnePackage include:

- sOneLandingPage

Other packages, including third party packages not developed by sequelone, may also depend on sOnePackage.
